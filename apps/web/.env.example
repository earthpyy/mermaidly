# Base URL of the short-link API worker (apps/api).
# In dev, the composable defaults to http://localhost:8787 (wrangler dev) when unset.
# In production, set this to the deployed worker URL or short links will fall back to
# inline self-contained links.
VITE_API_BASE=https://mermaidly-api.example.workers.dev
